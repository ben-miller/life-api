type Query {
    sources: Source
}

type Source {
    airtable(forceRefresh: Boolean = false): Airtable
    trello(forceRefresh: Boolean = false): Trello
    google(forceRefresh: Boolean = false): Google
    tidal(forceRefresh: Boolean = false): Tidal
    obsidian(forceRefresh: Boolean = false): Obsidian
    org_mode(forceRefresh: Boolean = false): OrgMode
    firefox(forceRefresh: Boolean = false): Firefox
    calendar(forceRefresh: Boolean = false): Calendar
    youtube(forceRefresh: Boolean = false): Youtube
}

type Airtable {
    job_search: JobSearch
}

type JobSearch {
    ignored_applications: Int
    rejected_applications: Int
    rejected_after_phone_screening: Int
    rejected_after_technical_screening: Int
    rejected_after_full_interview: Int
    total_rejections: Int
    in_progress: Int
    total_sent: Int
    interested: Int
}

type Firefox {
    bookmarks: Int
}

type Obsidian {
    inboxes: Int
    inbox_items: Int
}

type OrgMode {
    inboxes: Int
    inbox_items: Int
}

type Trello {
    inbox_size: Int
}

type Google {
    youtube: Youtube
}

type Youtube {
    liked_videos: Int
}

type Calendar {
    dev_total_hours: Float
    chores_total_hours: Float
    meditation_total_hours: Float
    running_total_hours: Float
    weight_training_total_hours: Float
    dev_total_sessions: Int
    chores_total_sessions: Int
    meditation_total_sessions: Int
    running_total_sessions: Int
    weight_training_total_sessions: Int
}

type Tidal {
    uncategorized_tracks: Int
}

schema {
    query: Query
}
